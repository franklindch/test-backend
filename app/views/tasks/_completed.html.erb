<% if task.completed? %>
  <div class="badge badge-success h-25">Completed</div>
<% else %>
  <%= link_to '<div class="badge badge-danger h-25">Pending</div>'.html_safe, completed_task_path(task) %>
<% end %>
